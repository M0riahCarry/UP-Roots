# Copy this file to ".env" (in the server/ folder) and fill in your key.
# server/.env is gitignored, so the key stays off GitHub and out of the browser.

PERENUAL_API_KEY=your_perenual_api_key_here

# Optional: the port the API listens on (defaults to 3001)
PORT=3001
